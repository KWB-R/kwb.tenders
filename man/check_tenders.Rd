% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/check_tenders.R
\name{check_tenders}
\alias{check_tenders}
\title{Check Vergabemarktplatz Brandenburg for relevant tenders}
\usage{
check_tenders(
  dir = "reports",
  headless = TRUE,
  login = FALSE,
  max_pages = Inf,
  publication_types = c("ExAnte", "Tender"),
  contracting_rules = "VOL",
  screen_details = TRUE,
  max_detail = Inf,
  username = Sys.getenv("VMP_BB_USERNAME"),
  password = Sys.getenv("VMP_BB_PASSWORD"),
  keywords = tender_keywords()
)
}
\arguments{
\item{dir}{Output directory for the report (default \code{"reports"}).}

\item{headless}{Kept for API compatibility (chromote runs headless).}

\item{login}{Log in before scraping (default \code{FALSE}; the search is public).}

\item{max_pages}{Maximum number of result pages to scrape (default \code{Inf}).}

\item{publication_types, contracting_rules}{Search filter passed to
\code{\link[=vmp_bb_scrape_tenders]{vmp_bb_scrape_tenders()}}. Defaults to Beabsichtigte Ausschreibung +
Ausschreibung (\code{c("ExAnte", "Tender")}) and VgV / VOL/A / UVgO (\code{"VOL"}).}

\item{screen_details}{Second relevance layer: fetch each ongoing tender's
public detail page and match its full text + CPV codes (default \code{TRUE}).
See \code{\link[=enrich_with_details]{enrich_with_details()}}.}

\item{max_detail}{Maximum number of detail pages to screen (default \code{Inf}).}

\item{username, password}{Credentials used when \code{login = TRUE} (default env
vars \code{VMP_BB_USERNAME} / \code{VMP_BB_PASSWORD}).}

\item{keywords}{Keyword list for relevance scoring (default
\code{\link[=tender_keywords]{tender_keywords()}}).}
}
\value{
Invisibly, the scored tibble of all tenders.
}
\description{
End-to-end pipeline: open a chromote session, (optionally) log in, scrape all
tenders, score them for relevance and write the overview report. This is the
function called by the scheduled GitHub Action.
}
\details{
The public tender search works \strong{without} login, so \code{login = FALSE} by
default. Set \code{login = TRUE} once you have valid credentials.
}
\examples{
\dontrun{
check_tenders() # public search, all pages
check_tenders(max_pages = 2) # quick test
}
}
