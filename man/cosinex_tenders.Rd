% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cosinex.R
\name{cosinex_tenders}
\alias{cosinex_tenders}
\title{Scrape + score a cosinex Vergabemarktplatz instance (generic connector)}
\usage{
cosinex_tenders(
  base_url,
  plattform,
  slug,
  mount = "VMPCenter",
  keywords = tender_keywords(),
  login = FALSE,
  max_pages = Inf,
  since_days = NULL,
  publication_types = c("ExAnte", "Tender"),
  contracting_rules = "VOL",
  screen_details = TRUE,
  max_detail = Inf,
  screen_notice = FALSE,
  max_notice = Inf,
  username = "",
  password = "",
  cache_dir = "reports",
  relevant_only = FALSE,
  headless = TRUE
)
}
\arguments{
\item{base_url}{Portal host, e.g. \code{"https://www.evergabe.nrw.de"}.}

\item{plattform}{Display name written to the \code{Plattform} column.}

\item{slug}{Short id used for the per-portal cache files (e.g. \code{"vmp_nrw"}).}

\item{mount}{cosinex mount segment: \code{"VMPCenter"} (Land marketplaces) or
\code{"Center"} (DTVP).}

\item{keywords}{Keyword list for relevance scoring (default \code{\link[=tender_keywords]{tender_keywords()}}).}

\item{login}{Log in before scraping (default \code{FALSE}; the search is public).}

\item{max_pages}{Maximum number of result pages to scrape (default \code{Inf}).}

\item{since_days}{If set, stop paging once a result page is entirely older
than this many days (the search is sorted newest-first). Bounds the scrape
for large portals/award histories; \code{NULL} scrapes up to \code{max_pages}. The
precise date trim happens later in \code{\link[=screen_portals]{screen_portals()}}.}

\item{publication_types, contracting_rules}{Search filter passed to
\code{\link[=vmp_bb_scrape_tenders]{vmp_bb_scrape_tenders()}}.}

\item{screen_details}{Detail-page layer (default \code{TRUE}; see
\code{\link[=enrich_with_details]{enrich_with_details()}}).}

\item{max_detail}{Maximum number of detail pages to screen (default \code{Inf}).}

\item{screen_notice}{Notice-PDF layer (default \code{FALSE}; forces \code{login = TRUE};
see \code{\link[=enrich_with_notice]{enrich_with_notice()}}).}

\item{max_notice}{Maximum number of new notice PDFs to read (default \code{Inf}).}

\item{username, password}{Credentials when \code{login = TRUE} (default env vars
\code{VMP_BB_USERNAME} / \code{VMP_BB_PASSWORD}).}

\item{cache_dir}{Directory for the detail/notice caches (default \code{"reports"}).}

\item{relevant_only}{Return only relevant tenders (default \code{FALSE}; the combined
multi-portal run in \code{\link[=screen_all_portals]{screen_all_portals()}} sets this \code{TRUE}).}

\item{headless}{Run chromote headless (default \code{TRUE}).}
}
\value{
A scored tibble with a \code{Plattform} column.
}
\description{
Shared engine behind \code{\link[=vmp_bb_tenders]{vmp_bb_tenders()}}, \code{\link[=vmp_nrw_tenders]{vmp_nrw_tenders()}} and
\code{\link[=dtvp_tenders]{dtvp_tenders()}}: opens a chromote session, optionally logs in, scrapes the
extended-search results, scores them (\code{\link[=score_relevance]{score_relevance()}}), enriches via the
detail (and optional notice) layers, applies the title/CPV exclusions
(\code{\link[=apply_title_excludes]{apply_title_excludes()}}) and tags \code{Plattform = plattform}. The detail and
notice caches are namespaced by \code{slug}, so several portals can share one
\code{cache_dir} without clobbering each other.
}
\examples{
\dontrun{
cosinex_tenders("https://www.evergabe.nrw.de", "Vergabemarktplatz NRW",
                slug = "vmp_nrw", max_pages = 2)
}
}
