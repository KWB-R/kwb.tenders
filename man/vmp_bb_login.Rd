% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/vmp_bb.R
\name{vmp_bb_login}
\alias{vmp_bb_login}
\title{Log in to Vergabemarktplatz Brandenburg (optional)}
\usage{
vmp_bb_login(
  session,
  username = Sys.getenv("VMP_BB_USERNAME"),
  password = Sys.getenv("VMP_BB_PASSWORD"),
  auth_url = VMP_BB_AUTH_URL
)
}
\arguments{
\item{session}{A session from \code{\link[=vmp_bb_session]{vmp_bb_session()}}.}

\item{username, password}{Credentials (default env vars \code{VMP_BB_USERNAME} /
\code{VMP_BB_PASSWORD}).}

\item{auth_url}{Login (Keycloak SSO) URL (default the Brandenburg one; other
cosinex portals pass their own via \code{cosinex_urls()}).}
}
\value{
The \code{session}, invisibly. Errors if the login is rejected.
}
\description{
Logs in via the Keycloak SSO form. Note: the public tender search works
\emph{without} login (see \code{\link[=vmp_bb_scrape_tenders]{vmp_bb_scrape_tenders()}}), so logging in is optional.
}
\examples{
\dontrun{
session <- vmp_bb_session()
vmp_bb_login(session)
}
}
