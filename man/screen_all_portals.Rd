% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/portals.R
\name{screen_all_portals}
\alias{screen_all_portals}
\title{Screen all configured portals into one combined report}
\usage{
screen_all_portals(
  dir = "reports",
  vmp_bb = TRUE,
  nrw = TRUE,
  dtvp = TRUE,
  berlin = TRUE,
  oeffentlichevergabe = TRUE,
  ted = TRUE,
  servicebund = TRUE,
  evergabe_online = TRUE,
  deutsche_evergabe = TRUE,
  vmp_bb_login = FALSE,
  vmp_bb_notice = FALSE,
  nrw_login = FALSE,
  nrw_notice = FALSE,
  since_days = 30,
  cosinex_contracting_rules = "VOL",
  keywords = tender_keywords(),
  verbose = TRUE
)
}
\arguments{
\item{dir}{Output directory (default \code{"reports"}).}

\item{vmp_bb, nrw, dtvp, berlin, oeffentlichevergabe, ted, servicebund}{Enable each
source (all \code{TRUE}).}

\item{evergabe_online}{Enable the evergabe-online.de connector (default
\code{TRUE}; login-free Wicket scrape, \code{\link[=evergabe_online_tenders]{evergabe_online_tenders()}}). Adds
below-threshold federal/Land/Kommunal notices not in the Datenservice.}

\item{deutsche_evergabe}{Enable the deutsche-evergabe.de connector (default
\code{TRUE}; login-free chromote render, \code{\link[=deutsche_evergabe_tenders]{deutsche_evergabe_tenders()}}). Covers
planned, current and awarded notices via the dashboard category switch.}

\item{vmp_bb_login, vmp_bb_notice}{Log in / read notice PDFs for VMP-BB
(default \code{FALSE}; need \verb{VMP_BB_*} secrets).}

\item{nrw_login, nrw_notice}{Log in / read notice PDFs for Vergabemarktplatz NRW
(default \code{FALSE}; need an NRW account + \verb{VMP_NRW_*} secrets).}

\item{since_days}{Unified look-back window in days, applied to every portal by
publication date (default \code{30}): the API connectors fetch this many days and a
final filter trims all sources (incl. VMP-BB) to the same window.}

\item{cosinex_contracting_rules}{Procurement regulations (Vergabeart) for the
cosinex portals (Brandenburg/NRW/DTVP), default \code{"VOL"} (VgV / VOL/A / UVgO;
excludes VOB/Bau). See \code{\link[=vmp_bb_scrape_tenders]{vmp_bb_scrape_tenders()}} for other values. The API
portals have no such filter (construction is excluded via the CPV-45 veto).}

\item{keywords}{Keyword groups (default \code{\link[=tender_keywords]{tender_keywords()}}).}

\item{verbose}{Print progress (default \code{TRUE}).}
}
\value{
Invisibly, the combined scored tibble.
}
\description{
Convenience entry point (used by the scheduled GitHub Action): wires the
built-in connectors -- the cosinex marketplaces Vergabemarktplatz Brandenburg
(\code{\link[=vmp_bb_tenders]{vmp_bb_tenders()}}), Vergabemarktplatz NRW (\code{\link[=vmp_nrw_tenders]{vmp_nrw_tenders()}}) and DTVP
(\code{\link[=dtvp_tenders]{dtvp_tenders()}}), Vergabeplattform Berlin (\code{\link[=berlin_tenders]{berlin_tenders()}}), the federal
Datenservice (\code{\link[=oeffentlichevergabe_tenders]{oeffentlichevergabe_tenders()}}), TED (\code{\link[=ted_tenders]{ted_tenders()}}) and the
service portal (\code{\link[=servicebund_tenders]{servicebund_tenders()}}) -- and runs them through
\code{\link[=screen_portals]{screen_portals()}}. The
searches are login-free (only VMP-BB optionally logs in for the notice layer),
and a portal that fails is skipped (the others still produce the report).
}
\examples{
\dontrun{
screen_all_portals(vmp_bb_login = TRUE, vmp_bb_notice = TRUE)
}
}
