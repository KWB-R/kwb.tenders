% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/portals.R
\name{combine_tenders}
\alias{combine_tenders}
\title{Combine scored tender tibbles from several portal connectors}
\usage{
combine_tenders(tenders_list)
}
\arguments{
\item{tenders_list}{A list of data frames (one per portal). \code{NULL} entries and
zero-row frames are dropped.}
}
\value{
One combined data frame (an empty data frame if all inputs are empty).
}
\description{
Row-binds the per-portal results, filling columns absent in some sources with
\code{NA}, and guarantees a \code{Plattform} column. Each input should be a scored tibble
(see \code{\link[=score_relevance]{score_relevance()}}) as returned by a portal connector.
}
\examples{
a <- data.frame(Plattform = "A", Kurzbezeichnung = "x", stringsAsFactors = FALSE)
b <- data.frame(Plattform = "B", cpv = "71351500-8", stringsAsFactors = FALSE)
combine_tenders(list(a, b))
}
