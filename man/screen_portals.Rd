% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/portals.R
\name{screen_portals}
\alias{screen_portals}
\title{Run several portal connectors, combine and write one report}
\usage{
screen_portals(
  sources,
  dir = "reports",
  portal = "tenders",
  keywords = tender_keywords(),
  keep_types = c("Ausschreibung", "Geplante Ausschreibung", "Vergebener Auftrag"),
  verbose = TRUE
)
}
\arguments{
\item{sources}{A named list of functions, each returning a scored tibble
(e.g. \code{list("TED" = function() ted_tenders())}). The name is used as the
\code{Plattform} if the connector does not set one.}

\item{dir}{Output directory (default \code{"reports"}).}

\item{portal}{File-name id for the combined report (default \code{"tenders"}).}

\item{keywords}{Passed to connectors that take it (currently informational).}

\item{keep_types}{Keep only these \code{Veroeffentlichungstyp} values (default:
Ausschreibung, Geplante Ausschreibung and Vergebener Auftrag -> own section
each). \code{NULL} keeps all types.}

\item{verbose}{Print progress (default \code{TRUE}).}
}
\value{
Invisibly, the combined scored tibble.
}
\description{
Calls each source connector (a function returning a scored tender tibble),
tagging it with a \code{Plattform}, combines the results with \code{\link[=combine_tenders]{combine_tenders()}}
and writes one report via \code{\link[=write_tender_report]{write_tender_report()}}. A source that errors is
logged and skipped, so one portal failing does not abort the run.
}
\examples{
\dontrun{
screen_portals(list(
  "Oeffentliche Vergabe" = function() oeffentlichevergabe_tenders(days = 7),
  "TED" = function() ted_tenders()
))
}
}
