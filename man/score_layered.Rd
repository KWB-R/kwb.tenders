% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/relevance.R
\name{score_layered}
\alias{score_layered}
\title{Layered relevance scoring for portal connectors (title + long text + CPV)}
\usage{
score_layered(
  df,
  title_cols,
  text_cols = character(),
  cpv_col = NULL,
  keywords = tender_keywords(),
  cpv_map = tender_cpv_map(),
  exclude = TRUE
)
}
\arguments{
\item{df}{A data frame of tenders.}

\item{title_cols}{Columns scored with the full rule (e.g. the title).}

\item{text_cols}{Columns scored strong-only (e.g. description); default none.}

\item{cpv_col}{Name of a comma/space-separated CPV column, or \code{NULL}.}

\item{keywords}{Keyword groups (default \code{\link[=tender_keywords]{tender_keywords()}}).}

\item{cpv_map}{CPV-to-group map (default \code{\link[=tender_cpv_map]{tender_cpv_map()}}).}

\item{exclude}{Apply \code{\link[=apply_title_excludes]{apply_title_excludes()}} afterwards to drop construction
/ building / maintenance tenders (default \code{TRUE}).}
}
\value{
\code{df} with \code{groups}, \code{cpv_groups}, \code{match_source}, \code{score},
\code{is_relevant} added, sorted by descending score.
}
\description{
Scores a tender tibble the way the VMP-BB pipeline does, but in one call for
connectors that already ship a description and CPV codes (e.g. the API
portals): \code{title_cols} use the full rule (>=1 strong OR >=2 supporting),
\code{text_cols} (long free text) are matched STRONG-only (incidental supporting
hits in long text are noise), and \code{cpv_col} codes are mapped to groups. The
three group sets are merged into \code{groups}, with \code{match_source}
(title/detail/cpv), \code{cpv_groups}, \code{score} and \code{is_relevant}.
}
