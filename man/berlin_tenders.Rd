% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/berlin.R
\name{berlin_tenders}
\alias{berlin_tenders}
\title{Vergabeplattform Berlin connector (HTTP, login-free)}
\usage{
berlin_tenders(
  keywords = tender_keywords(),
  cpv_map = tender_cpv_map(),
  since_days = 30,
  max_pages = 60,
  relevant_only = TRUE,
  verbose = TRUE
)
}
\arguments{
\item{keywords}{Keyword groups (default \code{\link[=tender_keywords]{tender_keywords()}}).}

\item{cpv_map}{CPV-to-group map (default \code{\link[=tender_cpv_map]{tender_cpv_map()}}).}

\item{since_days}{Stop paging once a page is entirely older than this many days
(the list is newest-first; default \code{30}). \code{NULL} pages up to \code{max_pages}.}

\item{max_pages}{Safety cap on pages fetched (default \code{60}; 10 notices/page).}

\item{relevant_only}{Return only relevant tenders (default \code{TRUE}).}

\item{verbose}{Print progress (default \code{TRUE}).}
}
\value{
A scored tibble with \code{Plattform = "Vergabeplattform Berlin"}.
}
\description{
Reads the Berlin notices (berlin.de, iTWO tender backend) over HTTP and scores
them (\code{\link[=score_layered]{score_layered()}}). The paginated HTML list (\code{?start=N}) is the primary
source: it covers the full look-back window and carries the iTWO detail link
per notice in a \code{data-href} attribute, with a date-based early stop. If the
HTML cannot be parsed it falls back to the RSS feed (latest ~50), which is also
used to backfill any missing links. No browser and no login required.
}
\examples{
\dontrun{
berlin_tenders(since_days = 30)
}
}
