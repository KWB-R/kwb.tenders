% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ted.R
\name{ted_tenders}
\alias{ted_tenders}
\title{Screen TED (Tenders Electronic Daily) for relevant tenders}
\usage{
ted_tenders(
  keywords = tender_keywords(),
  cpv_map = tender_cpv_map(),
  terms = ted_default_terms(),
  countries = "DEU",
  since_days = 90,
  scope = "ACTIVE",
  max_pages = 5,
  page_size = 100,
  relevant_only = TRUE,
  verbose = TRUE
)
}
\arguments{
\item{keywords}{Keyword groups (default \code{\link[=tender_keywords]{tender_keywords()}}).}

\item{cpv_map}{CPV-to-group map (default \code{\link[=tender_cpv_map]{tender_cpv_map()}}).}

\item{terms}{Full-text query terms (default: a built-in water-term set).}

\item{countries}{Place-of-performance country codes (default \code{"DEU"};
\code{NULL}/\code{character()} for EU-wide).}

\item{since_days}{Only notices published within the last N days (default \code{90},
via TED \code{today(-N)}); \code{NULL} to disable. Past-deadline notices are dropped too.}

\item{scope}{Notice scope (\code{"ACTIVE"}, \code{"ALL"}, \code{"LATEST"}; default \code{"ACTIVE"}).}

\item{max_pages, page_size}{Pagination caps (default \code{5} x \code{100}).}

\item{relevant_only}{Keep only relevant tenders (default \code{TRUE}).}

\item{verbose}{Print progress (default \code{TRUE}).}
}
\value{
A scored tibble of (relevant) tenders; empty data frame if none.
}
\description{
Login-free EU connector. Full-text queries \code{terms} (German water terms)
restricted to \code{countries}, fetches matching notices and scores them with
\code{\link[=score_layered]{score_layered()}}. Returns relevant tenders with a \code{Plattform} column.
}
\examples{
\dontrun{
ted_tenders(max_pages = 1)
}
}
