% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/notice.R
\name{tender_notice_text}
\alias{tender_notice_text}
\title{Fetch and extract the text of a tender's announcement (notice) PDF(s)}
\usage{
tender_notice_text(session, detail_url, max_pdfs = 3)
}
\arguments{
\item{session}{A logged-in session from \code{\link[=vmp_bb_session]{vmp_bb_session()}}.}

\item{detail_url}{The tender's detail URL (the \code{Aktion} column).}

\item{max_pdfs}{Maximum number of PDFs to read per tender (default \code{3}).}
}
\value{
The combined PDF text (empty string if none/!accessible).
}
\description{
Opens the (logged-in) detail page, finds the published Bekanntmachung PDF
link(s) and returns their combined extracted text. Requires a logged-in
session (see \code{\link[=vmp_bb_login]{vmp_bb_login()}}); no bidder registration needed.
}
\examples{
\dontrun{
s <- vmp_bb_session(); vmp_bb_login(s)
tender_notice_text(s, tenders$Aktion[1])
}
}
