% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/report.R
\name{write_tender_report}
\alias{write_tender_report}
\title{Write a tender overview report (Excel + Markdown + HTML)}
\usage{
write_tender_report(
  tenders,
  dir = "reports",
  portal = "vmp-bb",
  date = Sys.Date()
)
}
\arguments{
\item{tenders}{A scored tibble (see \code{\link[=score_relevance]{score_relevance()}}).}

\item{dir}{Output directory (created if needed). Default \code{"reports"}.}

\item{portal}{Short portal id used in file names. Default \code{"vmp-bb"}.}

\item{date}{Report date (default \code{Sys.Date()}).}
}
\value{
Invisibly, a list with the written file paths and counts.
}
\description{
Writes a dated Excel workbook (sheets "Relevant", "Alle", "Neu"), a
\code{latest.md} summary, a browsable \code{latest.html} (for GitHub Pages) and a small
state file used to flag tenders that are new since the previous run.
}
\examples{
\dontrun{
tenders <- score_relevance(vmp_bb_scrape_tenders(session))
write_tender_report(tenders)
}
}
