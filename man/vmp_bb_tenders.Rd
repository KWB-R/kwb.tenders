% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/check_tenders.R
\name{vmp_bb_tenders}
\alias{vmp_bb_tenders}
\title{Scrape + score Vergabemarktplatz Brandenburg (portal connector)}
\usage{
vmp_bb_tenders(
  keywords = tender_keywords(),
  login = FALSE,
  max_pages = Inf,
  publication_types = c("ExAnte", "Tender"),
  contracting_rules = "VOL",
  screen_details = TRUE,
  max_detail = Inf,
  screen_notice = FALSE,
  max_notice = Inf,
  username = Sys.getenv("VMP_BB_USERNAME"),
  password = Sys.getenv("VMP_BB_PASSWORD"),
  cache_dir = "reports",
  headless = TRUE
)
}
\arguments{
\item{keywords}{Keyword list for relevance scoring (default \code{\link[=tender_keywords]{tender_keywords()}}).}

\item{login}{Log in before scraping (default \code{FALSE}; the search is public).}

\item{max_pages}{Maximum number of result pages to scrape (default \code{Inf}).}

\item{publication_types, contracting_rules}{Search filter passed to
\code{\link[=vmp_bb_scrape_tenders]{vmp_bb_scrape_tenders()}}.}

\item{screen_details}{Detail-page layer (default \code{TRUE}; see
\code{\link[=enrich_with_details]{enrich_with_details()}}).}

\item{max_detail}{Maximum number of detail pages to screen (default \code{Inf}).}

\item{screen_notice}{Notice-PDF layer (default \code{FALSE}; forces \code{login = TRUE};
see \code{\link[=enrich_with_notice]{enrich_with_notice()}}).}

\item{max_notice}{Maximum number of new notice PDFs to read (default \code{Inf}).}

\item{username, password}{Credentials when \code{login = TRUE} (default env vars
\code{VMP_BB_USERNAME} / \code{VMP_BB_PASSWORD}).}

\item{cache_dir}{Directory for the detail/notice caches (default \code{"reports"}).}

\item{headless}{Run chromote headless (default \code{TRUE}).}
}
\value{
A scored tibble with a \code{Plattform} column.
}
\description{
The VMP-BB connector for \code{\link[=screen_portals]{screen_portals()}} / \code{\link[=screen_all_portals]{screen_all_portals()}}: opens a
chromote session, optionally logs in, scrapes tenders, scores them
(\code{\link[=score_relevance]{score_relevance()}}), enriches via the detail and (optional) notice layers,
applies the title exclusions (\code{\link[=apply_title_excludes]{apply_title_excludes()}}) and tags
\code{Plattform = "Vergabe Brandenburg"}. Returns the scored tibble (it writes no
report); the detail/notice screening caches are read/written under \code{cache_dir}.
}
\examples{
\dontrun{
vmp_bb_tenders(max_pages = 2)
}
}
