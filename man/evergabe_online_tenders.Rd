% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/evergabe_online.R
\name{evergabe_online_tenders}
\alias{evergabe_online_tenders}
\title{e-Vergabe des Bundes (evergabe-online.de) connector (HTTP, login-free)}
\usage{
evergabe_online_tenders(
  keywords = tender_keywords(),
  cpv_map = tender_cpv_map(),
  query = NULL,
  date_range = "TWENTY_EIGHT_DAYS",
  max_pages = 30,
  max_query_chars = 90,
  relevant_only = TRUE,
  verbose = TRUE
)
}
\arguments{
\item{keywords}{Keyword groups (default \code{\link[=tender_keywords]{tender_keywords()}}).}

\item{cpv_map}{CPV-to-group map (default \code{\link[=tender_cpv_map]{tender_cpv_map()}}).}

\item{query}{Full-text query; default is the strong KWB keywords joined with
\code{" | "} (the portal treats \code{|} as OR).}

\item{date_range}{Portal publish-date window: one of \code{"ALL"}, \code{"SEVEN_DAYS"},
\code{"FOURTEEN_DAYS"}, \code{"TWENTY_ONE_DAYS"}, \code{"TWENTY_EIGHT_DAYS"}
(default \code{"TWENTY_EIGHT_DAYS"}).}

\item{max_pages}{Page cap per batch (10 hits/page) (default 30).}

\item{max_query_chars}{Max length of each OR-batch query (default 90). The
portal silently returns nothing for long queries (~>110 chars), so the
strong keywords are packed into short OR-batches (each <= this) and the
hits are unioned.}

\item{relevant_only}{Return only relevant tenders (default \code{TRUE}).}

\item{verbose}{Print progress (default \code{TRUE}).}
}
\value{
A scored tibble with \code{Plattform = "e-Vergabe des Bundes (evergabe-online.de)"}.
}
\description{
Searches the federal procurement platform evergabe-online.de for the KWB
keywords and scores the hits (\code{\link[=score_layered]{score_layered()}}). Adds \emph{below-threshold}
federal notices that the Datenservice (oeffentlichevergabe.de) does not carry.
Driven login-free over httr (Apache-Wicket form POST + Wicket-Ajax paging); no
CPV/free text in the result list, so relevance is title-based.
}
\examples{
\dontrun{
evergabe_online_tenders(date_range = "SEVEN_DAYS")
}
}
