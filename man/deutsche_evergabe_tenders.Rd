% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/deutsche_evergabe.R
\name{deutsche_evergabe_tenders}
\alias{deutsche_evergabe_tenders}
\title{Deutsche eVergabe (deutsche-evergabe.de) connector (chromote, login-free)}
\usage{
deutsche_evergabe_tenders(
  keywords = tender_keywords(),
  cpv_map = tender_cpv_map(),
  terms = NULL,
  status = c("aktuell", "geplant", "vergeben"),
  max_detail = Inf,
  relevant_only = TRUE,
  verbose = TRUE
)
}
\arguments{
\item{keywords}{Keyword groups (default \code{\link[=tender_keywords]{tender_keywords()}}).}

\item{cpv_map}{CPV-to-group map (default \code{\link[=tender_cpv_map]{tender_cpv_map()}}).}

\item{terms}{Search terms (default: minimal roots of the strong KWB keywords,
\code{\link[=tender_keywords]{tender_keywords()}}). The grid is searched per term (DevExpress
\code{searchByText}) and the hits are unioned.}

\item{status}{Which notice classes to query via the dashboard category
dropdown: \code{"aktuell"} (open tenders), \code{"geplant"} (Vorinformationen) and/or
\code{"vergeben"} (Zuschlagsbekanntmachungen). Default: all three.}

\item{max_detail}{Cap on relevant tenders to enrich via the detail endpoint
(default \code{Inf}).}

\item{relevant_only}{Return only relevant tenders (default \code{TRUE}).}

\item{verbose}{Print progress (default \code{TRUE}).}
}
\value{
A scored tibble with \code{Plattform = "Deutsche eVergabe (deutsche-evergabe.de)"}.
}
\description{
Renders the public Healy-Hudson dashboard grid with chromote, pages through it
and scores the hits (\code{\link[=score_layered]{score_layered()}}); relevant tenders are enriched via the
login-free per-tender detail endpoint (Vergabestelle / Publikationsdatum /
Angebotsfrist / CPV). No API/RSS exists, so a browser render is required; the
result list carries no CPV/free text, so relevance is title-based.
}
\examples{
\dontrun{
deutsche_evergabe_tenders(status = "aktuell")
}
}
