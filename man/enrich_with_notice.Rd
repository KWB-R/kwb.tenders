% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/notice.R
\name{enrich_with_notice}
\alias{enrich_with_notice}
\title{Enrich tenders with a notice-PDF (Bekanntmachung) relevance layer}
\usage{
enrich_with_notice(
  session,
  tenders,
  keywords = tender_keywords(),
  ongoing_only = TRUE,
  max_notice = Inf,
  delay = 0.3,
  cache = NULL
)
}
\arguments{
\item{session}{A logged-in session from \code{\link[=vmp_bb_session]{vmp_bb_session()}}.}

\item{tenders}{A tibble (typically already passed through
\code{\link[=enrich_with_details]{enrich_with_details()}}).}

\item{keywords}{Keyword groups (default \code{\link[=tender_keywords]{tender_keywords()}}).}

\item{ongoing_only}{Only screen ongoing tenders (default \code{TRUE}).}

\item{max_notice}{Maximum number of \emph{new} notice PDFs to read (default \code{Inf}).}

\item{delay}{Seconds between tenders (default \code{0.3}).}

\item{cache}{Notice cache from a previous run (see \code{\link[=read_notice_cache]{read_notice_cache()}}).}
}
\value{
\code{tenders} with the notice layer merged in.
}
\description{
For ongoing tenders not yet cached, reads the published announcement PDF(s)
via the logged-in \code{session} and matches the keyword groups against the text.
Adds a \code{notice_groups} column, merges it into \code{groups}/\code{is_relevant} and adds
the \code{notice} source to \code{match_source}. Requires a logged-in session. The
updated cache is returned as \code{attr(result, "notice_cache")}.
}
