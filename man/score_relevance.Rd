% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/relevance.R
\name{score_relevance}
\alias{score_relevance}
\title{Score tenders for relevance to KWB research groups}
\usage{
score_relevance(tenders, keywords = tender_keywords())
}
\arguments{
\item{tenders}{A data frame / tibble of tenders (e.g. from
\code{\link[=vmp_bb_scrape_tenders]{vmp_bb_scrape_tenders()}}).}

\item{keywords}{Keyword groups (default \code{\link[=tender_keywords]{tender_keywords()}}). May also be a
single group as \code{list(strong = ..., supporting = ...)}.}
}
\value{
\code{tenders} with added columns \code{groups} (matching group names, comma
separated), \code{matched_keywords}, \code{score} and \code{is_relevant}, sorted by
descending score.
}
\description{
Case-insensitive substring matching of each group's keywords against all
character columns. A tender matches a group if it contains at least one
\code{strong} keyword or at least two \code{supporting} keywords; it is relevant if it
matches at least one group.
}
\examples{
df <- data.frame(
  Bezeichnung = c("Grundwassermonitoring Brunnen", "Kanalsanierung Sensorik"),
  stringsAsFactors = FALSE
)
res <- score_relevance(df)
res[, c("Bezeichnung", "groups", "score")]
}
